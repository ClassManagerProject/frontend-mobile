package com.example.primerlabcompose

/**
 * MIGRADO A NAVIGATION COMPOSE
 *
 * Esta Activity ha sido reemplazada por TaskDetailsScreen.kt (Composable).
 * La navegación ahora se gestiona con NavHost + NavController en AppNavigation.kt.
 *
 * Rutas de navegación:
 *   Calendario  →  Screen.Calendar  ("calendar")
 *   Detalles    →  Screen.TaskDetails  ("task_details/{taskId}")
 */

